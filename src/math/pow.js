/**
 * 거듭제곱 함수 (a의 b제곱)
 * @param {number} base - 밑
 * @param {number} exponent - 지수
 * @returns {number} base의 exponent제곱
 */
export function pow(base, exponent) {
    return Math.pow(base, exponent);
}

export default pow;
