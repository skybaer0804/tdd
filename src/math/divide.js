/**
 * 나눗셈 함수
 * @param {number} dividend - 나뉘는 수 (피제수)
 * @param {number} divisor - 나누는 수 (제수)
 * @returns {number} 나눗셈의 결과
 */
export function divide(dividend, divisor) {
    return dividend / divisor;
}

export default divide;
