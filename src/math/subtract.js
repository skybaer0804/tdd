/**
 * 두 숫자를 빼는 함수
 * @param {number} a - 첫 번째 숫자 (피감수)
 * @param {number} b - 두 번째 숫자 (감수)
 * @returns {number} 두 숫자의 차
 */
export function subtract(a, b) {
    return a - b;
}

export default subtract;
